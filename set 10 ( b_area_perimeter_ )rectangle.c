// 10 b) write a c program to compute area and perimeter of rectangle
#include<stdio.h>
void main()
{
	int h, w, a, p;
	h=7;
	w=5;
	a= h*w;
	p= 2 * (h+w);
	printf("\nArea of the rectange is %d \nPerimeter of the rectangle is %d", a, p);
}
